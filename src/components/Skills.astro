---
const frontEnd = [
  "HTML",
  "CSS / SCSS",
  "JavaScript (ES6+)",
  "TypeScript",
  "Angular 20+",
  "React.js 19+",
  "Astro",
  "Micro-Frontends",
];

const libraries = [
  "Angular Material",
  "PrimeNG",
  "Ionic 7+",
  "Git & GitHub",
  "RESTful APIs",
  "RxJs",
  "NgRx",
  "TanStack Query",
  "TanStack Table",
  "TanStack Form",
  "TanStack Router",
  "Tanstack Virtual",
  "Zod",
  "TailwindCSS",
  "Rspack",
  "Figma (Basic)",
];

const backEnd = ["Node.js (Learning)", "NestJS (Learning)", "Python", "Golang"];
---

<section>
  <h2 class="!mb-2 text-xl sm:text-2xl font-bold">Skills</h2>
  <h3 class="!mb-2">Frontend :</h3>
  <div
    class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 gap-2 mb-6 text-sm sm:text-base"
  >
    {
      frontEnd.map((skill) => {
        return (
          <span class="text-secondary-light dark:text-secondary-dark">
            {skill}
          </span>
        );
      })
    }
  </div>
  <h3 class="!mb-2">Libraries & Tools :</h3>
  <div
    class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 gap-2 mb-6 text-sm sm:text-base"
  >
    {
      libraries.map((skill) => {
        return (
          <span class="text-secondary-light dark:text-secondary-dark">
            {skill}
          </span>
        );
      })
    }
  </div>
  <h3 class="!mb-2">BackEnd - (Basic Experience) :</h3>
  <div
    class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 gap-2 text-sm sm:text-base"
  >
    {
      backEnd.map((skill) => {
        return (
          <span class="text-secondary-light dark:text-secondary-dark">
            {skill}
          </span>
        );
      })
    }
  </div>
</section>
