---
import Social from "../components/Social.astro";
import BaseLayout from "../layouts/BaseLayout.astro";
import Skills from "../components/Skills.astro";
import { MAIN_SECTION } from "../consts";
import Meetup from "../components/Meetup.astro";
import OpenSource from "../components/OpenSource.astro";
---

<BaseLayout>
  <section class="flex flex-col gap-6 sm:gap-8 px-4 sm:px-6">
    <div class="w-full max-w-2xl mx-auto flex flex-col min-w-0">
      <h1 class="text-xl sm:text-2xl font-semibold">Hey!!, I'm Abiram 👋🏻😊</h1>
      {
        MAIN_SECTION.map((section) => {
          return (
            <p class="mt-4 text-base sm:text-lg text-secondary-light dark:text-secondary-dark">
              👉🏻 {section}
            </p>
          );
        })
      }
    </div>
    <div class="w-full max-w-2xl mx-auto flex flex-col min-w-0">
      <Skills />
    </div>
    <div class="w-full max-w-2xl mx-auto sm:px-0">
      <OpenSource />
    </div>
    <div class="w-full max-w-2xl mx-auto sm:px-0">
      <Meetup />
    </div>
    <div class="w-full max-w-2xl mx-auto sm:px-0">
      <Social />
    </div>
  </section>
</BaseLayout>
